# RNApp

## Steps
- cd `apps/RNApp`
- `bundle install`로 ruby package를 인스톨 해주세요.
- `cd ios`, `bundle exec pod install --verbose`로 Pods를 인스톨 해주세요.

## Misc
- RN 0.81.4를 쓰고 있어요 (추후, 안정화된 버전이 발견되면 업데이트 할 예정이에요.)
- monorepo에 맞추어 android, Pod 설정 등을 하고 있어요
