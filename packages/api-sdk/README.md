# prisma-schema

## What
1. swagger를 이용한, openapi 형식의 typescript codegen을 만들어 주어요.

## 환경설정

1. `.env.sample`에 있는 환경 변수에 값을 채워넣어 `.env`를 생성후에 사용주세요!
2. `pnpm swagger:dev`, `pnpm swagger:prod`를 통해 dev, prod 환경 등의 stage를 자유롭게 조절하시면 되어요.
